The big olive shopping-mode checkoff. Fills olive with a white check when checked.

```jsx
<Checkbox checked={item.checked} onChange={next => toggle(item.id, next)} />
```

`onChange` receives the next boolean. Default size 28px — bump for an even larger shelf tap target. Pair with `GroceryItemRow` (which embeds one) for full rows.
